spring:
  data:
    mongodb:
       uri: mongodb://${MONGO_HOST:localhost}:27017/ct

server:
 host: 0.0.0.0
 port: 8080
 contextPath: /stock-exchange-service

foresight:
  cron:
      refresh: "*/60 * * * * *"